# Contributing

Keep runtime uploads, local environments, and API keys out of Git. Set `OPENWEATHER_API_KEY` in your environment when testing the optional weather panel.

Do not replace or retrain `plantDiseaseDetection.h5` without updating the notebook, class labels, and documented evaluation evidence together.
